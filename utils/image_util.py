from PIL import Image, ImageDraw, ImageFont

def make_carousel_slide(title, subtitle, myth, truth, background, style="default"):
    # Generate/compose the image as shown in your samples
    # Save or return file path
    pass